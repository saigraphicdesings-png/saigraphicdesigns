(function(){"use strict";const $=id=>document.getElementById(id);const authView=$("authView"),profileView=$("profileView"),loginForm=$("loginForm"),signupForm=$("signupForm"),loginTab=$("loginTab"),signupTab=$("signupTab"),authMessage=$("authMessage");function message(el,text,type){el.textContent=text||"";el.className="account-message"+(type?" "+type:"")}function mode(which){const login=which==="login";loginForm.classList.toggle("is-hidden",!login);signupForm.classList.toggle("is-hidden",login);loginTab.classList.toggle("active",login);signupTab.classList.toggle("active",!login);message(authMessage,"")}async function api(path,options){const response=await fetch(path,{credentials:"same-origin",headers:{"Content-Type":"application/json",...(options&&options.headers||{})},...options});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.error||"Something went wrong. Please try again.");return data}function showProfile(user){authView.classList.add("is-hidden");profileView.classList.remove("is-hidden");$("profileName").textContent=(user.name||"Customer").split(" ")[0];$("profileFullName").textContent=user.name||"—";$("profileEmail").textContent=user.email||"—";$("profileCreated").textContent=user.createdAt?new Date(user.createdAt+(/Z$/.test(user.createdAt)?"":"Z")).toLocaleDateString():"—"}function showAuth(){profileView.classList.add("is-hidden");authView.classList.remove("is-hidden");mode("login")}loginTab.addEventListener("click",()=>mode("login"));signupTab.addEventListener("click",()=>mode("signup"));loginForm.addEventListener("submit",async e=>{e.preventDefault();message(authMessage,"Signing in…");const form=new FormData(loginForm);try{const data=await api("/api/auth/login",{method:"POST",body:JSON.stringify({email:form.get("email"),password:form.get("password")})});showProfile(data.user)}catch(err){message(authMessage,err.message,"error")}});signupForm.addEventListener("submit",async e=>{e.preventDefault();message(authMessage,"Creating your account…");const form=new FormData(signupForm);try{const data=await api("/api/auth/signup",{method:"POST",body:JSON.stringify({name:form.get("name"),email:form.get("email"),password:form.get("password")})});showProfile(data.user)}catch(err){message(authMessage,err.message,"error")}});$("logoutBtn").addEventListener("click",async()=>{try{await api("/api/auth/logout",{method:"POST",body:"{}"});showAuth();message(authMessage,"You have been logged out.","success")}catch(err){message($("profileMessage"),err.message,"error")}});(async()=>{try{const data=await api("/api/auth/me",{method:"GET"});if(data.user)showProfile(data.user);else showAuth()}catch{showAuth()}})()})();