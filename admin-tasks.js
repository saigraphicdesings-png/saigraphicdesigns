(()=>{const token=()=>sessionStorage.getItem("saiShopAdminToken")||"";const api=(path,opts={})=>fetch(path,{...opts,headers:{Authorization:"Bearer "+token(),"content-type":"application/json",...(opts.headers||{})}});const $=id=>document.getElementById(id);let tasks=[];const esc=v=>String(v||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));function show(message,error=false){const el=$("taskMessage");el.textContent=message;el.style.color=error?"#be123c":"#047857";}function render(){const filter=$("taskFilter").value,rows=filter==="all"?tasks:tasks.filter(t=>t.status===filter),wrap=$("taskCards");if(!rows.length){wrap.innerHTML='<p class="empty">No tasks here yet.</p>';return}wrap.innerHTML=rows.map(t=>'<article class="task-card"><div><span class="status '+esc(t.status)+'">'+esc(t.status)+'</span><h3>'+esc(t.taskTitle)+'</h3><div class="task-meta"><span>👤 '+esc(t.customerName)+'</span>'+(t.customerPhone?'<span>☎ '+esc(t.customerPhone)+'</span>':'')+'<span>📅 End: '+esc(t.endDate)+'</span>'+(t.reminderSentAt?'<span>✓ Reminder sent</span>':'')+'</div>'+(t.notes?'<p class="task-notes">'+esc(t.notes)+'</p>':'')+'</div><div class="task-actions">'+(t.status==="open"?'<button class="complete" data-action="complete" data-id="'+t.id+'">Complete</button>':'<button class="reopen" data-action="reopen" data-id="'+t.id+'">Reopen</button>')+'<button class="secondary" data-action="edit" data-id="'+t.id+'">Edit</button><button class="remove" data-action="delete" data-id="'+t.id+'">Delete</button></div></article>').join("")}async function load(){if(!token()){location.href="admin.html";return}const r=await api("/api/admin/tasks");if(!r.ok){$("taskCards").innerHTML='<p class="empty">Could not load tasks.</p>';return}tasks=(await r.json()).tasks||[];render()}$("taskForm").addEventListener("submit",async e=>{e.preventDefault();const id=$("taskId").value,data={customerName:$("customerName").value,customerPhone:$("customerPhone").value,taskTitle:$("taskTitle").value,endDate:$("endDate").value,notes:$("taskNotes").value};show("Saving…");const r=await api(id?"/api/admin/tasks/"+id:"/api/admin/tasks",{method:id?"PATCH":"POST",body:JSON.stringify(data)}),d=await r.json().catch(()=>({}));if(!r.ok){show(d.error||"Could not save task.",true);return}$("taskForm").reset();$("taskId").value="";$("formTitle").textContent="Add Task";show("Task saved.");await load()});$("taskFilter").addEventListener("change",render);$("taskCards").addEventListener("click",async e=>{const btn=e.target.closest("button[data-id]");if(!btn)return;const t=tasks.find(x=>x.id===btn.dataset.id);if(!t)return;const action=btn.dataset.action;if(action==="edit"){$("taskId").value=t.id;$("customerName").value=t.customerName;$("customerPhone").value=t.customerPhone;$("taskTitle").value=t.taskTitle;$("endDate").value=t.endDate;$("taskNotes").value=t.notes;$("formTitle").textContent="Edit Task";window.scrollTo({top:0,behavior:"smooth"});return}if(action==="delete"){if(!confirm("Delete this task?"))return;await api("/api/admin/tasks/"+t.id,{method:"DELETE"});await load();return}await api("/api/admin/tasks/"+t.id,{method:"PATCH",body:JSON.stringify({customerName:t.customerName,customerPhone:t.customerPhone,taskTitle:t.taskTitle,endDate:t.endDate,notes:t.notes,status:action==="complete"?"completed":"open"})});await load()});load()})();